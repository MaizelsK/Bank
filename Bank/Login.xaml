<Window x:Class="Bank.Login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bank"
        mc:Ignorable="d"
        Title="Login" Height="250" Width="250">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>

        <Label Content="Авторизация" HorizontalAlignment="Center" FontSize="20"/>
        <StackPanel Orientation="Vertical" Grid.Row="1" HorizontalAlignment="Center" Grid.ColumnSpan="2">
            <Label Content="Имя пользователя"/>
            <TextBox Name="LoginText" Width="150"/>

            <Label Content="Пароль"/>
            <TextBox Name="PasswordText" Width="150"/>

            <Button Name="LoginButton" Content="Login" Width="80" Margin="0,5"/>
            <Button Name="RegisterButton" Content="Register" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
